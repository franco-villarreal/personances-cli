export const enum MainMenuOptions {
  INSERT_RECORD = "INSERT RECORD",
  DELETE_RECORD = "DELETE RECORD",
  LIST_RECORDS = "LIST RECORDS",
  BALANCES = "BALANCES",
  EXIT = "EXIT",
}

export const enum RecordType {
  INCOME = "INCOME",
  EXPENSE = "EXPENSE",
}

export const enum ReportsMenuOption {
  HISTORIC = "HISTORIC",
  DATE_RANGE = "DATE RANGE",
  MONTH_AND_YEAR = "MONTH AND YEAR",
}

export const enum RecordCategory {
  ANY = "ANY",
  GLOCERY = "GLOCERY",
  SERVICES = "SERVICES",
}
